---
name: ats-resume-optimizer
description: Optimize resumes for ATS (Applicant Tracking Systems) by analyzing job descriptions and tailoring resume content with relevant keywords while preserving original structure, length, and bullet point counts. Use when Claude needs to: (1) help users tailor their resume for specific job applications, (2) improve resume keyword matching for ATS systems, or (3) generate ATS-compatible resume formats in both JSON and document outputs.
skillId: skp-capctdo8dqv6h4p33wk5vtrs
workflowId: see-workflow-mapping
version: 1.0.0
---

# ats-resume-optimizer

Optimize resumes for ATS (Applicant Tracking Systems) by analyzing job descriptions and tailoring resume content with relevant keywords while preserving original structure, length, and bullet point counts. Use when Claude needs to: (1) help users tailor their resume for specific job applications, (2) improve resume keyword matching for ATS systems, or (3) generate ATS-compatible resume formats in both JSON and document outputs.

## Installation

```bash
refly skill install skp-capctdo8dqv6h4p33wk5vtrs
```

## Usage

After installation, run the skill using your installation ID:

```bash
refly skill run <installationId> --input '{}'
```

The installation ID is returned when you run `refly skill install`.
